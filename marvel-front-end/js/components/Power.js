export default function Power(power) {

    return `
        <h3 class="power__powerName item">${power.powerName}</h3>
        <p class="power__description item">${power.description}</p>
            `
}
